{
  "scorer": {
    "chain": ["laya-local", "jev-typesafe", "jev-fallback"],
    "config": {},
    "note": "Laya (local, free) → Jev TypeSafe (network, paid) → jev-fallback (unknowns). Swap chain order or remove entries as needed."
  },
  "router": {
    "provider": "keyword-router",
    "config": {},
    "note": "Skill routing handled by superpowers (in-context). Keyword-router kept as Layer 2 contract implementation; not called from hooks."
  },
  "memory": {
    "provider": "filesystem-fallback",
    "config": {}
  },
  "device": {
    "chain": ["argent", "artemis", "device-fallback"],
    "config": {}
  },
  "session": {
    "provider": "herdr",
    "config": {}
  },
  "dev": {
    "defaultVia": "flowmo"
  }
}
